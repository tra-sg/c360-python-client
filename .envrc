# .envrc for discreetly

# Prefer pyenv over the python3 layout
#   Requires use_python() to be defined in ~/.config/direnv/direnvrc
#   see: https://github.com/direnv/direnv/wiki/Python#pyenv
if has pyenv; then
  layout pyenv 3.7.10
else
  layout python
fi


export C360_TENANT="test"
export C360_STAGE="dev"
# the library defaults to prod
# but this repo's environment default to dev
# for testing purposes
